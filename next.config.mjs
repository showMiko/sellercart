/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['firebasestorage.googleapis.com', 'example.com', 'anotherdomain.net'],
      },
};

export default nextConfig;
